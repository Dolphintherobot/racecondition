# racecondition
A website that you can post your programming questions to.
Enables creation of accounts, questions,replies and can add photos to them.

Frontend is done via react 
Backend is done with nodejs, uses cluster module to enable concurrency.
Database is a mysql database.

to run pull down the project and then type docker compose build then docker compose up
